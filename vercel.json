{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && cd nodejs-ai-assistant && npm install --include=dev && cd ../react-stream-ai-assistant && npm install --include=dev",
  "buildCommand": "cd nodejs-ai-assistant && npm run build && cd ../react-stream-ai-assistant && npm run build",
  "outputDirectory": "react-stream-ai-assistant/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 300
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
