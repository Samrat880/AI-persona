# Stream Chat — loaded at runtime from backend /api/public-config (do NOT put API keys here)
# Backend URL
# Local dev: http://localhost:3000
# Vercel (same domain): leave empty — API calls use /api/*
VITE_BACKEND_URL=http://localhost:3000
